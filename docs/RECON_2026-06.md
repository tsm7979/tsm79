# TSM Repo Recon — 2026-06

Deep scan of every TSM-related location (local + GitHub) before the backend
"deterministic trust membrane" rebuild. Snapshot of reality, not aspiration.

## Locations

| Location | Git | Role | Keep? |
|---|---|---|---|
| **`Desktop/TSMv1`** | `tsm7979/tsm79` (canonical, live) | The polyglot backend + landing. Rust 75 / Python 62 / Go 14 / Java 16 / C# 5 / C 9 / eBPF. | **CANONICAL — all backend work happens here** |
| `Desktop/TSMv1_test` | `tsm7979/tsm79` @ Apr-04 (stale) | Original pre-polyglot Python firewall (Python 139). | Archive / port-in reference |
| `Desktop/SecOps-ai` | `noData0079/SecOPS.v1` | ML + model home. 24k files; **`tsm-COREv0.gguf` 4.6 GB**, `model.safetensors` 1.2 GB, installers. Python 5903. | Model + training artifacts (keep, separate) |
| `Desktop/APP` | none | TSM99 app prototype — TypeScript 1935 + Python 520. | UI prototype reference |
| `Desktop/TSM-Install-Test` | none | PyInstaller bundle (TF/torch DLLs + GGUF). 8.8k files. | Built artifact — not source |
| `Desktop/model` | none | Distillation/training scripts: 4 Kaggle notebooks, teacher/student dirs, GRPO + SCRATCH+VERIFY pipeline for Qwen3.5-9B teacher. | Training pipeline (keep, separate) |
| `Desktop/frontend` | none | One 97 MB `.exe` installer. | Artifact — ignore |
| `Desktop/frontend assets` | none | Scraped commercial site mirrors + licensed fonts. | Reference only — never ship |

## Canonical backend (`TSMv1`) layer map

| Layer | Lang | Files | Notes |
|---|---|---|---|
| `dataplane/` | Rust | 77 | The inline firewall / hot path |
| `detector/` | Python | 23 | ML detector (gRPC + HTTP) |
| `control-plane/` | Go | 9 | config / workspace / keys |
| `threat-intel/` | Go | 4 | IP reputation feeds |
| `overlay-node/` | Go (+1 py) | 2 | libp2p `.tsm` DHT |
| `admin-api/` | Java | 16 | operator REST (Spring Boot) |
| `policy-lsp/` | C# | 5 | policy DSL language server |
| `edge/` | C++ (+Rust) | 4 | wasmtime worker host |
| `ebpf` + loaders | C / Rust | 13 | XDP/TC packet authority |
| `tsm/` | Python | 22 | SDK + CLI (reference impl) |
| `tests/` | Python | 13 | cross-component |

## Verified build/health status (2026-06)

Verified build status per layer (2026-06, on WSL Ubuntu / host):

| Layer | Lang | Build | How verified |
|---|---|---|---|
| `dataplane/` | Rust | ✅ **0 errors**, 296 warns, 15.3s | `cargo check --bins`, WSL, rustc **1.96.0** |
| `detector/` + `tsm/` | Python | ✅ imports + 305 tests green | pytest, host |
| `control-plane/` | Go | ✅ build + vet clean | `go build/vet ./...`, host |
| `threat-intel/` | Go | ✅ build clean | host |
| `overlay-node/` | Go | ✅ build clean **(fixed: go.sum was missing)** | host, after `go mod tidy` |
| `edge/` | C++ | ✅ `edge_host` binary built (32 MB, wasmtime static) | cmake + wasmtime, WSL |
| `admin-api/` | Java | ⏳ verifying (Maven) | WSL |
| `policy-lsp/` | C# | ⏳ needs .NET SDK | pending |

**The ICE story (corrected):** rustc 1.94.0 AND 1.95.0 both ICE on the dataplane
crate (panic in `early_lint_checks`). #36 misdiagnosed it as 1.95-only and pinned
1.94.0 — but 1.94.0 ICEs too. The dataplane SOURCE was always fine. **rustc
1.96.0 (2026-05-25) fixes the ICE**; the dataplane compiles green. `rust-toolchain.toml`
now pins 1.96.0. The dataplane had never compiled on any toolchain before this.

- **Incompleteness markers**: 43 TODO/FIXME/stub across the backend, concentrated
  in `dataplane/src/{audit/postgres,pool/connection,tls/keys}.rs` and the
  generated `detector/gen/detect_pb2_grpc.py`. (296 dataplane warnings = mostly
  unused imports — cosmetic, cleanup pending.)
- **Build-env gaps found**: WSL had no Rust/Go/Java/Maven (now: Rust 1.96 + Go +
  C++ toolchain + wasmtime + Maven installing). Host has Java 21 but no Maven;
  repo ships `mvnw` (Unix) but no `mvnw.cmd` (Windows) — a real portability gap.

## Target architecture (this phase)

Per the new direction: **TSM is a deterministic trust membrane around AI, not an
AI.** AI is an untrusted upstream compute endpoint. The live gate is rules,
crypto, state, and human governance.

Bidirectional membrane:

```
Client → INGRESS (authn/mTLS, tenant bind, rate limit, normalize)
       → deterministic pipeline (normalize → signatures → structural →
         entropy → context rules → policy engine → decision)
       → {block | redact | route_local | quarantine | allow}
       → AI provider / local model / human queue
       → EGRESS (response scan: secret echo, PII leak, injection-back,
         cross-tenant) → audit-sign → Client
```

Seven deterministic layers to complete/harden:
1. Signature detection (regex / structural / checksum) — exists, harden
2. Entropy analysis — exists, harden
3. Policy engine (signed DSL → IR → dataplane) — partial
4. Rule-based content classification (jailbreak phrase families + normalization) — partial
5. Reputation DB (hashed known-bad) — **missing**
6. Human quarantine (verdict exists; review API missing — GH #38)
7. Sovereign overlay security (Ed25519 / signed records / Kademlia / TLS / Merkle / RBAC) — slice 1+2 done

Plus the missing **egress plane** (response inspection) — the highest-leverage gap.
